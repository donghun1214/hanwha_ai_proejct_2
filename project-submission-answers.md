# AI 기반 현장 작업 일정 최적화 서비스 제출 답변

## 1. 해결할 문제와 대상

### 누가 어떤 상황에서 어떤 문제를 겪고 있나요?

이 프로젝트의 대상은 한화에어로스페이스 현장에서 다음 날 작업 일정을 계획하고 작업자를 배치해야 하는 현장 관리자와 작업 반장입니다.

현장 관리자는 매일 다음 날 진행할 작업 목록을 확인하고, 각 작업에 필요한 인원과 작업자 역량, 작업 위치, 작업 위험도, 날씨 조건을 함께 고려해 작업 순서를 정해야 합니다.

특히 외부 설비 점검, 펌프 보수, 배관 교체처럼 실외에서 진행되는 작업은 비, 바람, 체감온도에 영향을 크게 받습니다. 그래서 단순히 작업 우선순위만 보고 배치할 수 없고, 날씨가 안전한 시간대에 외부 작업을 배치해야 합니다.

하지만 기존 방식은 관리자가 작업 정보와 날씨 정보를 따로 확인한 뒤 직접 판단하는 구조이기 때문에 시간이 오래 걸리고, 작업자 중복 배정이나 위험 시간대 배치 같은 문제가 발생할 수 있습니다.

## 2. 현재 상황과 불편

### 기존 방식의 반복, 대기, 오류, 정보 단절

기존 방식에서는 다음과 같은 불편이 있습니다.

- 관리자가 작업 목록을 직접 확인하고 작업 순서를 수동으로 정리해야 합니다.
- 작업별 필요 인원, 예상 작업 시간, 작업 위치, 위험도, 작업자 역량을 일일이 비교해야 합니다.
- 외부 작업의 경우 기상청 예보나 날씨 정보를 따로 확인한 뒤 작업 가능 여부를 판단해야 합니다.
- 작업자가 동시에 여러 작업에 배정되지 않았는지 직접 확인해야 합니다.
- 작업 순서가 정해진 뒤에는 작업 반장님께 보낼 메일이나 문자 내용을 다시 작성해야 합니다.
- 작업 내용, 배정 인원, 주의 사항이 여러 곳에 흩어져 있어 정보가 한 번에 연결되지 않습니다.

이 과정은 매일 반복되는 업무입니다. 작업 수가 늘어나거나 날씨가 좋지 않은 날에는 판단해야 할 조건이 많아져 대기 시간이 길어지고, 관리자가 실수로 작업을 누락하거나 같은 작업자를 겹치는 시간대에 배정할 가능성도 커집니다.

따라서 현재 문제는 단순히 일정 작성이 번거로운 수준이 아니라, 반복 업무로 인한 시간 낭비, 판단 기준 분산, 정보 전달 지연, 배정 오류 가능성이 함께 존재한다는 점입니다.

## 3. 활용 자료·데이터

### 출처와 범위

이 프로젝트에서 활용한 자료와 데이터는 다음과 같습니다.

1. 현장 작업 샘플 데이터

- 외부 설비 점검
- 설비 보수 작업
- 내부 설비 점검
- 자재 및 작업 기록 정리
- 배관 교체 및 압력 시험

각 작업에는 작업명, 작업 대상, 외부/내부 작업 여부, 예상 소요 시간, 필요 인원, 우선순위, 작업 내용, 최소 작업 역량을 포함했습니다.

2. 기상청 단기예보 공개 데이터

- 공공데이터포털에서 제공하는 기상청 단기예보 API를 활용했습니다.
- 한화에어로스페이스 여수 사업장 주변 위치를 기준으로 시간대별 예보를 가져오도록 구성했습니다.
- 활용 범위는 내일 작업 시간대인 09:00, 10:00, 11:00, 13:00, 14:00, 15:00, 16:00입니다.
- 사용한 기상 요소는 기온, 습도, 강수량, 풍속이며, 이를 바탕으로 체감온도를 계산해 작업 위험도 판단에 반영했습니다.

## 4. 사용한 AI·자동화 도구

사용한 도구: ChatGPT Codex

AI·자동화 도구: OpenAI API, SOLAPI 문자 발송 기능(발신번호 인증 단계 보류)

## 5. AI·자동화 흐름

### 입력 -> AI 처리 -> 자동화 -> 사람의 확인 -> 출력

1. 입력

관리자는 웹 화면에서 다음 정보를 입력하거나 조정합니다.

- 내일 진행할 작업 목록
- 작업별 위치, 예상 시간, 필요 인원, 우선순위, 최소 작업 역량
- 총 작업 가능 인원
- 작업자별 역량

서비스는 추가로 기상청 단기예보 API에서 내일 시간대별 날씨를 가져옵니다.

2. AI 처리

OpenAI API는 입력된 작업 정보, 작업자 정보, 가용 인원, 시간대별 기상 데이터를 함께 분석합니다.

AI는 다음 조건을 고려해 작업 일정을 추천합니다.

- 외부 작업은 강수량, 체감온도, 풍속이 높을수록 보수적으로 판단
- 내부 작업은 날씨 영향이 낮으므로 위험 시간대에 우선 배치 가능
- 작업별 필요 인원과 최소 작업 역량 충족 여부 확인
- 같은 작업자가 겹치는 시간대에 중복 배정되지 않도록 고려
- 작업이 18시 이전에 끝나는지 확인

3. 자동화

AI가 추천한 결과는 서버에서 다시 검증됩니다.

- 작업이 누락되거나 중복되지 않았는지 확인
- 허용된 시간대에 배치되었는지 확인
- 위험 점수가 정상 범위인지 확인
- 작업자 중복 배정이 있는지 확인
- 필요한 인원이 부족하면 해당 작업을 중단 검토로 조정

이 검증 과정을 통해 AI 응답을 그대로 사용하는 것이 아니라, 실제 서비스에서 사용할 수 있는 형태로 다시 확인합니다.

4. 사람의 확인

관리자는 화면에서 AI 추천 결과를 확인합니다.

- 작업별 시작 시간과 종료 시간
- 작업 위험도
- 진행, 주의, 내부 우선, 중단 검토 판단
- 배정된 작업자
- 추천 이유

관리자는 AI 추천 근거를 보고 실제 현장 상황에 맞게 최종 판단할 수 있습니다.

5. 출력

최종적으로 서비스는 작업 보드와 공유용 문자 내용을 제공합니다.

- 작업 보드에서는 내일 작업 순서를 한눈에 확인할 수 있습니다.
- 작업 상세 화면에서는 각 작업의 판단 근거를 확인할 수 있습니다.
- 문자 미리보기에서는 작업 반장님께 전달할 일정과 주의 사항을 자동으로 정리해 줍니다.

## 6. 만든 과정

### 역할분담

프로젝트는 다음 역할을 기준으로 나누어 진행할 수 있도록 구성했습니다.

- 기획: 현장 작업 배치의 문제 상황 정리, 필요한 입력값과 출력값 정의
- 프론트엔드: 작업 등록 화면, 공수 관리 화면, 작업 보드, 기상 예보 화면 구현
- AI 로직: 작업 정보와 기상 정보를 AI에 전달하는 프롬프트와 응답 형식 설계
- 데이터 연동: 기상청 단기예보 API 연결 및 시간대별 날씨 데이터 가공
- 검증: AI 추천 결과의 작업 누락, 중복 배정, 시간 오류, 위험도 오류 확인
- 발표 준비: 문제 정의, 서비스 흐름, 시연 시나리오, 발표 스크립트 작성

### 주요 제작 단계

1. 문제 정의

현장 작업 배치가 수작업으로 이루어져 시간이 오래 걸리고, 날씨와 위험도 판단이 분산되어 있다는 문제를 정의했습니다.

2. 입력 데이터 설계

작업 배치에 필요한 정보를 정리했습니다.

- 작업명
- 작업 장소
- 외부/내부 작업 여부
- 예상 작업 시간
- 필요 인원
- 우선순위
- 최소 작업 역량
- 작업자별 역량
- 시간대별 기상 정보

3. 화면 구현

관리자가 한 화면에서 작업과 공수를 조정하고, AI 추천 결과를 확인할 수 있도록 웹 화면을 구현했습니다.

4. 기상 데이터 연동

기상청 단기예보 API를 사용해 내일 작업 시간대의 기상 정보를 가져오도록 만들었습니다.

5. AI 일정 추천 구현

OpenAI API를 사용해 작업 정보, 공수 정보, 날씨 정보를 함께 분석하고, 작업별 시작 시간, 종료 시간, 위험도, 판단 결과, 배정 인원, 추천 이유를 생성하도록 구성했습니다.

6. 서버 검증 로직 구현

AI 응답이 실제 일정으로 사용할 수 있는지 다시 확인하는 검증 로직을 추가했습니다.

7. 공유 문구 생성

AI가 추천한 일정과 주의 사항을 작업 반장님께 전달할 수 있도록 문자 미리보기 형태로 자동 정리했습니다.

### 시행착오와 검증 방법

제작 과정에서 가장 중요했던 점은 AI가 만든 결과를 그대로 믿지 않고, 서비스 내부에서 다시 검증하는 것이었습니다.

AI는 자연어 판단에는 강점이 있지만, 실제 일정에서는 작업 누락, 중복 배정, 잘못된 시간대 선택 같은 문제가 생기면 안 됩니다. 그래서 다음 검증을 추가했습니다.

- 작업 수가 입력과 결과에서 동일한지 확인
- 작업 번호가 중복되거나 누락되지 않았는지 확인
- 시작 시간이 실제 기상 예보 시간대 안에 있는지 확인
- 작업 종료 시간이 18시를 넘지 않는지 확인
- 위험 점수가 0~100 범위 안에 있는지 확인
- 같은 작업자가 같은 시간대에 중복 배정되지 않았는지 확인
- 최소 작업 역량을 만족하는 작업자만 배정되는지 확인

또한 `npm.cmd run build`, `npm.cmd run lint`, `node scripts/test-schedule.mjs`로 구현 상태를 확인했습니다. 빌드는 성공했고, 일정 추천 검증 테스트도 통과했습니다. lint에서는 실행을 막는 오류는 없었고, 사용하지 않는 상태 변수와 이미지 최적화 관련 경고만 확인되었습니다.

## 7. 효과와 활용 가치

### 확인한 변화와 기대효과

1. 시간 절감

관리자가 작업 목록, 날씨, 인력 현황을 따로 확인하고 직접 정리하던 과정을 한 화면에서 처리할 수 있습니다. 작업 등록 후 AI 추천 버튼을 누르면 작업 순서와 배정 결과가 자동으로 생성되므로 일정 작성 시간이 줄어듭니다.

2. 품질 향상

기존에는 관리자의 경험에 의존해 판단하던 작업 순서에 날씨, 위험도, 필요 인원, 작업자 역량이라는 기준을 명확하게 반영할 수 있습니다.

3. 오류 감소

서버 검증 로직을 통해 작업 누락, 중복 배정, 잘못된 시간대 선택, 인원 부족 같은 오류 가능성을 줄일 수 있습니다.

4. 현장 경험 개선

작업 반장님은 정리된 작업 일정과 주의 사항을 받아볼 수 있고, 관리자는 전달 문구를 새로 작성하는 부담을 줄일 수 있습니다.

5. 확산 가능성

현재는 한화에어로스페이스 여수 사업장 기준으로 구성했지만, 다른 사업장이나 다른 현장 업무에도 적용할 수 있습니다. 위치 좌표, 작업 종류, 작업자 정보, 판단 기준만 바꾸면 설비 점검, 건설 현장, 물류 현장, 안전 점검 업무에도 확장할 수 있습니다.

## 8. 한계·배운 점·다음 계획

### 막힌 점과 한계

- 실제 문자 발송 기능은 발신번호 인증이 필요한 외부 서비스 연동 문제로 현재는 미리보기까지만 구현했습니다.
- 기상청 API 키와 OpenAI API 키가 없으면 실제 예보 조회와 AI 추천 기능을 사용할 수 없습니다.
- 현재 작업 데이터와 작업자 데이터는 샘플 중심이므로 실제 현장에 적용하려면 실제 작업 코드, 근무조, 자격 정보와 연결해야 합니다.
- AI 추천 결과는 참고 자료이며, 최종 작업 결정은 현장 관리자가 확인해야 합니다.

### 배운 점

- AI를 현장 업무에 적용할 때는 단순히 답변을 생성하는 것보다 입력값과 출력값을 명확히 구조화하는 것이 중요하다는 점을 배웠습니다.
- 실제 업무 자동화에서는 AI 결과를 그대로 사용하는 것이 아니라, 서버에서 다시 검증하는 과정이 필요하다는 점을 확인했습니다.
- 날씨, 인력, 작업 위험도처럼 서로 다른 데이터를 연결하면 반복적인 판단 업무를 줄일 수 있다는 점을 배웠습니다.

### 다음 적용 계획

- 실제 문자 발송 API를 연동해 작업 반장님께 바로 전달할 수 있도록 개선
- 작업 일정 결과를 엑셀 또는 PDF로 내보내는 기능 추가
- 실제 현장 작업 이력 데이터를 저장해 반복 작업을 더 쉽게 불러오는 기능 추가
- 사업장별 위치와 작업 기준을 설정할 수 있는 관리자 화면 추가
- AI 추천 결과와 실제 현장 결정 결과를 비교해 추천 품질을 개선
